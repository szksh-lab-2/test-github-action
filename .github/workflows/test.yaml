---
name: test
on: pull_request
jobs:
  test:
    runs-on: ubuntu-24.04
    permissions: {}
    timeout-minutes: 10
    steps:
      - run: echo "hello"
        if: "false" # Thsi step is always skipped
        env:
          PAYLOAD: ${{ fromJSON('') }} # This always causes an error
